# Changelog

本文件记录 EchoLens Agent 的公开版本变化。

## [Unreleased]

### Added

- 增加 `/pwd`、`/cd <path>` 和 `/workspace [path]` 命令，可在 TUI 与行模式中查看或切换工作目录。
- 增加工作区运行时管理器和回归测试，覆盖相对路径、空格路径、同目录去重及初始化失败回退。
- 增加 TUI Slash 命令候选菜单：统一命令目录、说明过滤、别名、参数补全和键盘导航。

### Changed

- 工作目录切换时重新创建 Session、工具注册表、代码智能、MCP、审批存储、后台队列和子 Agent，并复用当前模型连接。
- 根 README 与公开功能链路文档补充工作目录切换的数据流、状态隔离和失败恢复说明。

## [0.7.0] - 2026-09-01

### Added

- 增加跨进程文件锁，并复用于 Session Event Store 和持久后台任务队列。
- 增加文件锁并发、失效所有者、释放重试和多队列实例写入回归测试。

### Changed

- Worktree 子 Agent 以过滤后的当前工作区快照作为初始基线，能够看到已修改、未跟踪和已删除文件。
- Worktree 变更统计改为对比快照，只报告子 Agent 相对当前工作区产生的变化。
- 工具 JSON Schema 使用共享的严格对象构造器，减少重复实现。
- TypeScript 开启未使用代码、未检查索引、隐式返回、Switch 穿透和 Override 等额外检查。
- 更新 `tsx` 和 `vscode-jsonrpc` 的补丁版本。

### Fixed

- 修复多个队列实例并发读改写时丢失后台任务的问题。
- 修复同一 Session 可被多个写者同时打开并破坏事件序列的问题。
- 修复 Worktree 子 Agent 看不到用户未提交工作区状态的问题。
- 修复 TUI 异常退出后可能残留备用屏幕或隐藏光标的问题。
- 修复 TUI 历史和转录数组访问可能越界的问题。
- 修复文件锁释放失败后无法再次清理的问题。

### Security

- 队列临时文件使用唯一名称并在失败路径清理，避免并发写入互相覆盖或残留。
- Worktree 分配失败时显式清理过滤快照和 Git Worktree，不静默忽略敏感工作副本残留。
